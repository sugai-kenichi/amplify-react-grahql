type Note @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  description: String
  image: String
  imagepath: String
}

type TestTable @model @auth(rules: [{ allow: public }]) {
  id: ID!
  firstName: String!
}
